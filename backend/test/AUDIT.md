# Test Audit: Overlap Analysis — contracts/stellar-save

Generated by: feature/deduplicate-backend-tests
Date: 2026-08-31

## Summary

| Module | Total tests before | Tests removed | Tests after |
|--------|--------------------|---------------|-------------|
| `payout_executor.rs` | 41 | 10 | 31 |
| `cycle_advancement.rs` | 8 | 0 | 8 |
| `payout.rs` | 9 | 0 | 9 |
| **Total** | **165** | **10** | **155** |

## Overlap 1 — Cycle advancement logic (HIGH duplication)

### Duplicated tests

| Test in `payout_executor.rs` | Duplicates test in `cycle_advancement.rs` | Verdict |
|------------------------------|------------------------------------------|---------|
| `test_advance_cycle_or_complete_valid` | `test_advance_group_cycle_logic_success` + `test_advance_group_cycle_logic_intermediate` | **Remove from payout_executor** |
| `test_advance_cycle_or_complete_to_completion` | `test_advance_group_cycle_logic_completion` + `test_advance_group_cycle_logic_multiple_times` | **Remove from payout_executor** |
| `test_advance_cycle_or_complete_small_group` | `test_advance_group_cycle_logic_completion` | **Remove from payout_executor** |
| `test_advance_cycle_or_complete_large_group` | `test_advance_group_cycle_logic_progression` | **Remove from payout_executor** |
| `test_advance_cycle_or_complete_increments_by_one` | `test_advance_group_cycle_logic_success` | **Remove from payout_executor** |
| `test_advance_cycle_or_complete_already_complete` | `test_advance_group_cycle_logic_error_on_already_complete` | **Remove from payout_executor** |

**Kept in `payout_executor.rs` (unique — tests storage wiring):**
- `test_advance_cycle_or_complete_storage_key` — verifies the group is persisted to the
  correct storage key after advancing; this is integration-level wiring not tested in
  `cycle_advancement.rs` (which tests pure logic with no storage dependency).

### Rationale

`cycle_advancement.rs::advance_group_cycle_logic` and
`payout_executor.rs::advance_cycle_or_complete` share the same cycle-increment logic.
The distinction is:
- **Unit level** (`cycle_advancement.rs`): tests the pure state-machine logic (no Env storage)
- **Integration level** (`payout_executor.rs`): should only test the storage persistence wiring

After deduplication, `payout_executor.rs` retains only the storage integration test.

---

## Overlap 2 — PayoutRecord validation on zero/negative amounts (MEDIUM duplication)

| Test in `payout_executor.rs` | Duplicates test in `payout.rs` | Verdict |
|------------------------------|-------------------------------|---------|
| `test_record_payout_zero_amount` | `test_invalid_amount` | **Remove from payout_executor** |
| `test_record_payout_negative_amount` | `test_invalid_amount` | **Remove from payout_executor** |
| `test_record_payout_validation` | `test_validate` | **Remove from payout_executor** |

### Rationale

`payout.rs` is the unit owner of `PayoutRecord` construction and validation.
Testing that `PayoutRecord::new` panics on amount ≤ 0 belongs there, not in
`payout_executor.rs` which is testing the executor's persistence wiring.
`payout_executor.rs` already covers the negative-amount path implicitly through
`test_record_payout_valid` (which would not compile if the struct were broken).

---

## Overlap 3 — No duplicates found in these modules

- `pool.rs` — tests `PoolInfo` and `calculate_total_pool` pure-logic functions; no overlap with other modules
- `group.rs` — tests `Group` struct lifecycle; no overlap
- `status.rs` — tests `GroupStatus` transitions; no overlap
- `helpers.rs` — tests format/deadline/cycle utility functions; no overlap
- `storage.rs` — tests `StorageKeyBuilder` key construction; no overlap
- `security.rs` — tests ed25519 input validation; no overlap
- `error.rs` — tests error codes and categories; no overlap
- `events.rs` — tests event emission; no overlap
- `contribution.rs` — tests `ContributionRecord`; no overlap
